apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend-dev.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "frontend-dev.labels" . | nindent 4 }}
data:
  README: |
    Replace this chart skeleton with the real frontend deployment config.
    Update image repository/tag, ingress host, env, and secret references.
