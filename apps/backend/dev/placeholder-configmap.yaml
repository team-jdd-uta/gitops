apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-manifest-placeholder
  namespace: backend
data:
  README: |
    Replace this placeholder with the real backend manifests, Helm values, or
    Kustomize overlay for the dev environment.
