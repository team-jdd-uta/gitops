apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend-dev.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "backend-dev.labels" . | nindent 4 }}
data:
  README: |
    Replace this chart skeleton with the real backend deployment config.
    Update image repository/tag, ingress host, env, and secret references.
